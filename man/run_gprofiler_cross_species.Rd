% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_gprofiler.R
\name{run_gprofiler_cross_species}
\alias{run_gprofiler_cross_species}
\title{Run g:Profiler enrichment across multiple species (customizable, cached GO depth)}
\usage{
run_gprofiler_cross_species(
  de_genes,
  bg_genes = NULL,
  species = c("hsapiens", "mmusculus", "rnorvegicus", "ggallus", "drerio",
    "dmelanogaster", "celegans"),
  outdir = "cross_species_gprofiler",
  do_no_bg = TRUE,
  sources = c("GO:BP", "GO:MF", "GO:CC", "KEGG"),
  user_threshold = 0.05,
  correction_method = "fdr",
  evcodes = TRUE,
  significant = FALSE,
  sleep_sec = 0.5,
  verbose = TRUE,
  go_obo = NULL
)
}
\arguments{
\item{de_genes}{character vector of EggNOG-mapped DE gene IDs.}

\item{bg_genes}{character vector of EggNOG-mapped background gene IDs (used when do_no_bg = FALSE for with_bg runs).}

\item{species}{character vector of g:Profiler organism codes (e.g., "hsapiens"). Unlimited.
Named vector allowed for pretty labels (names = codes, values = labels). If unnamed, labels=codes.}

\item{outdir}{output directory root (keeps your structure).}

\item{do_no_bg}{logical; if TRUE, also run no-background (genome-wide) analyses.}

\item{sources}{g:Profiler sources to query.}

\item{user_threshold}{numeric p-value threshold.}

\item{correction_method}{FDR method ("fdr","gSCS","bonferroni").}

\item{evcodes}{logical; return evidence codes.}

\item{significant}{logical; if TRUE g:Profiler filters to significant; we keep FALSE then filter ourselves as needed.}

\item{sleep_sec}{seconds to pause between species to avoid throttling.}

\item{verbose}{logical; print progress.}

\item{go_obo}{optional path to a GO OBO file; if NULL we auto-cache one per session.}
}
\value{
list with per-run data.frames and a `$paths` list of written files.
}
\description{
Run g:Profiler enrichment across multiple species (customizable, cached GO depth)
}
